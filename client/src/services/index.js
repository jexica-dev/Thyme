//define the api url
export const baseURL = `https://api.airtable.com/v0/appApGCWyst1pxnGO/leaves`;

//define const config object that has our authorization

export const config = {
  headers: {
    Authorization: `Bearer keyfNLPuV1hAUMPM1`,
  },
};
